import { StyleSheet } from "react-native";
import { Colors } from "~/constants/colors";

export const styles = StyleSheet.create({
  input: {
    fontSize: 16,
    paddingVertical: 12,
    padding: 16, // Adjusted to override default padding
    fontWeight: "500",
  },
});