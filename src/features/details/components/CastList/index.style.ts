import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    paddingHorizontal: 16,
    paddingVertical: 32,
    gap: 8
   },
  title: {
    fontWeight: 'bold',
    fontSize: 22,
  },
  list: {
    gap: 16
  }
});